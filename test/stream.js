/*
var Stream = require('../src/stream');
var assert = require('assert');
describe('stream',function(){
  describe('fromArray()', function(){
    var ar = [1,2,3];
    var s = Stream.fromArray(ar);
    assert.eq([1,2,3],s.toArray());
  });

  describe('#map()', function(){
    var ar = [1,2,3];
    var s = Stream.fromArray(ar).map(function(i){ return i * i});
    assert.eq([1,4,9],s.toArray());
  });
});
*/
